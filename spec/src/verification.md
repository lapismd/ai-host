# Verification

| ID           | Status      | Evidence                                                                                                                                                                                                                                                                                                                                |
| ------------ | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AH-GOV-001   | Implemented | `pnpm spec:build` builds the standalone mdBook.                                                                                                                                                                                                                                                                                         |
| AH-GOV-002   | Implemented | `pnpm spec:first` maps protected changes to canonical chapters.                                                                                                                                                                                                                                                                         |
| AH-GOV-003   | Implemented | `pnpm spec:validate` enforces unique definitions and one verification row per ID.                                                                                                                                                                                                                                                       |
| AH-GOV-004   | Implemented | `docs/HISTORY_MIGRATION.md` records the extraction and audit.                                                                                                                                                                                                                                                                           |
| AH-GOV-005   | Implemented | Focused path classification in `spec-validator.config.mjs`; root pnpm workspace policy no longer overrides the published validator package, AGENTS dependency guidance uses published ranges, and release/docs changes map to canonical chapters.                                                                                       |
| AH-PKG-007 | Implemented | `scripts/declarations.test.ts` builds the package and compiles a NodeNext consumer that must reject invalid public agent and executor inputs. |
| AH-PKG-001   | Implemented | Root `package.json` names public `@lapismd/ai-host@0.1.0`, exports built `dist` entrypoints for the root library, `./client`, and `./file-tools`, exposes `build`, `check`, `test`, `lapis-ai-host`, and `lapis-mcp-shim`, and resolves `@lapismd/spec-validator` as an npm development dependency.                                     |
| AH-PKG-002   | Implemented | `src/mcp-shim.ts` and `src/tool-bridge.ts` own the stdio shim, Streamable HTTP MCP, and broker; `src/file-tools` stays algorithm-only.                                                                                                                                                                                                  |
| AH-PKG-004   | Implemented | `.changeset/config.json`, `.github/workflows/release.yml`, and `scripts/ai-host-release.mjs` create future Changesets version PRs, rebuild `dist` before packing, build verified tarball artifacts, document the completed manual bootstrap, and reserve trusted publishing plus `ai-host@<version>` release notes for future versions. |
| AH-PKG-005   | Implemented | `src/ws-contract.test.ts` verifies that the controller profile exposes ACP discovery while rejecting raw process execution and caller MCP definitions.                                                                                                                                                                                  |
| AH-PKG-006   | Implemented | Serve tests suppress token output for an embedded caller, while the importable MCP shim and full launcher command are covered by package and tool-bridge tests.                                                                                                                                                                         |
| AH-FT-001    | Implemented | `package.json` publishes `./file-tools`; the leaf and its isolation test import no MCP, acpx, or transport modules.                                                                                                                                                                                                                     |
| AH-FT-002    | Implemented | File-tools tests accept `{ path, content }` write plans and leave path checks to the caller.                                                                                                                                                                                                                                            |
| AH-FT-003    | Implemented | File-tools tests hoist `edits[]`, flat old/new pairs, string aliases, `file_path`, and JSON-string `edits`.                                                                                                                                                                                                                             |
| AH-FT-004    | Implemented | File-tools tests cover unique, zero, repeated, overlapping, and no-op edit hunks.                                                                                                                                                                                                                                                       |
| AH-FT-005    | Implemented | File-tools tests parse V4A add, update, delete, and move envelopes and apply update hunks through injected operations.                                                                                                                                                                                                                  |
| AH-FT-006    | Implemented | Execute helpers take caller operations and a path resolver; no vault types appear in the leaf.                                                                                                                                                                                                                                          |
| AH-FT-007    | Implemented | `src/file-tools/NOTICE.md` retains the OpenClaw MIT copyright.                                                                                                                                                                                                                                                                          |
| AH-PKG-003   | Implemented | `scripts/serve-local.test.ts` seeds or reuses `.env` `LAPIS_AGENT_RUNTIME_TOKEN` and starts `serve`; `src/parse-cli.test.ts` preserves the library serve parser.                                                                                                                                                                        |
| AH-CLI-001   | Implemented | `src/parse-cli.test.ts` and `src/serve.ts` require a token and default to localhost.                                                                                                                                                                                                                                                    |
| AH-CLI-002   | Implemented | `src/serve.test.ts` covers embedded token output and suppression while retaining the returned token and URL.                                                                                                                                                                                                                            |
| AH-WS-001    | Implemented | `src/handshake.test.ts` rejects missing hello, a bad token, and a first command.                                                                                                                                                                                                                                                        |
| AH-WS-002    | Implemented | `src/protocol.ts` and `src/ws-contract.test.ts` use `desktop_agent_acp_*` commands and `agent-runtime-event` frames.                                                                                                                                                                                                                    |
| AH-WS-003    | Implemented | Executor and WebSocket tests reject pending commands on transport closure and emit one runtime error.                                                                                                                                                                                                                                   |
| AH-WS-004    | Implemented | `src/ws-contract.test.ts` asserts the Deno runtime identity, issues two commands on a fresh bridge, and shares one handshake.                                                                                                                                                                                                           |
| AH-PROTO-001 | Implemented | `src/replay-buffer.test.ts` and `src/ws-contract.test.ts` cover bounds, ordered subscribe, explicit-close cleanup, no prompt resend, and monotonic delivery of a new turn after a replacement host resets its in-memory sequence state.                                                                                                 |
| AH-ACP-001   | Implemented | `src/acp-session-options.test.ts` and executor tests keep sessions usable when thinking is unadvertised.                                                                                                                                                                                                                                |
| AH-ACP-002   | Implemented | Executor tests reuse matching live and caller-identified persisted sessions without starting or closing another runtime and preserve a successful disposable catalog when backend `session/close` is unsupported.                                                                                                                       |
| AH-ACP-003   | Implemented | ACP option and executor tests pass agent/model to acpx and select the advertised thinking, effort, or reasoning-effort key at session start.                                                                                                                                                                                            |
| AH-ACP-004   | Implemented | `src/acp-model-catalog.test.ts` and executor catalog tests return ids plus Cursor label and badges.                                                                                                                                                                                                                                     |
| AH-ACP-005   | Implemented | Executor restart-resume tests reuse the host id as sessionKey, omit acpx resumeSessionId, and the WebSocket replacement-host test verifies that an acknowledged continuation base advances new frames.                                                                                                                                  |
| AH-ACP-006   | Implemented | Session-option tests append a path-free available_skills manifest and omit path-bearing metadata.                                                                                                                                                                                                                                       |
| AH-ACP-007   | Implemented | Session-option tests append path-free sessionBootstrap with the skills manifest and omit path-bearing bootstrap text.                                                                                                                                                                                                                   |
| AH-ACP-008   | Implemented | Executor deferred-start tests keep initialization out of the native response microtask queue, reserve a stable id, queue a prompt until initialization completes, cancel pending prompts, surface one sequenced startup error, and retain awaited-start behavior.                                                                       |
| AH-ACP-009   | Implemented | Executor deferred-prompt tests reserve a run id synchronously and prove the ACP turn starts only in a later event-loop task.                                                                                                                                                                                                            |
| AH-ACP-010   | Implemented | Executor restricted-session tests prove one-shot isolation, empty MCP projection, immediate permission rejection, and temporary-state cleanup; Lapis desktop forwarding is covered by its consumer tests.                                                                                                                               |
| AH-ACP-011   | Implemented | Executor tests cover model and thinking aliases, effective-model verification, unsupported keys, set failures, and retained session usability.                                                                                                                                                                                          |
| AH-ACP-012   | Implemented | `src/acp-agent.test.ts`, executor tests and WebSocket contract tests cover defaults, configured custom commands, discovery, disabled and unknown identifiers, and MCP transport selection.                                                                                                                                              |
| AH-ACP-013   | Implemented | `src/executor.permissions.test.ts` and executor lifecycle tests cover exact offered choices, invalid decisions, pending status, cancellation, timeout, disconnect and close cleanup.                                                                                                                                                    |
| AH-ACP-014   | Implemented | Executor catalog tests project validated ACP configuration options from disposable session status together with the existing model catalog.                                                                                                                                                                                             |
| AH-PROTO-002 | Implemented | Protocol, WebSocket, client, and Deno consumer tests cover the v4 configure command, capability gating, and structured parity.                                                                                                                                                                                                          |
| AH-PROTO-003 | Implemented | WebSocket and executor tests cover discovery/status routing, caller-known persisted catalog lookup, and the controller command and MCP restrictions.                                                                                                                                                                                    |
| AH-PROTO-004 | Implemented | WebSocket client tests cover automatic compatibility, restored cursors, manual acknowledgement validation, deduplication and replay without prompt resubmission.                                                                                                                                                                        |
| AH-PROTO-005 | Planned     | No structured-elicitation command or event is exposed; permission tests remain limited to exact security outcomes.                                                                                                                                                                                                                      |
| AH-MCP-001   | Implemented | `src/tool-bridge.test.ts` covers v3 bridge messages, reserved `lapis-tools`, token authorization, and disconnect cancellation.                                                                                                                                                                                                          |
| AH-MCP-002   | Implemented | `src/executor.test.ts` projects MCP servers with `type: stdio` and required stdio fields.                                                                                                                                                                                                                                               |
| AH-MCP-003   | Implemented | Executor tests project Cursor `lapis-tools` as `type: http` with loopback URL and bearer headers; tool-bridge tests list and call through Streamable HTTP.                                                                                                                                                                              |
| AH-MCP-004   | Implemented | Tool-bridge tests bind an owner-selected loopback port and verify that HTTP and WebSocket contributions advertise it.                                                                                                                                                                                                                   |
| AH-MCP-005   | Implemented | Tool-bridge tests route an official SDK client through the attached Web-standard handler and reject missing bearer authorization.                                                                                                                                                                                                       |
| AH-MCP-006   | Implemented | Tool-bridge tests resolve the default stdio shim from source and built module locations without consulting the caller entrypoint; the real Codex ACP smoke lane starts `lapis-tools` from that launcher.                                                                                                                                |
| AH-MCP-007   | Implemented | Tool-bridge tests verify an embedding executable can supply the exact MCP shim command and arguments while bridge credentials remain environment-only.                                                                                                                                                                                  |
| AH-OPS-001   | Implemented | `src/cli/args.test.ts`; `deno task docs:check` verifies the generated command reference.                                                                                                                                                                                                                                                |
| AH-OPS-002   | Implemented | `src/cli/commands.test.ts`; `scripts/smoke-cli.ts` parses finite JSON and streamed NDJSON.                                                                                                                                                                                                                                              |
| AH-OPS-003   | Implemented | `src/cli/commands.test.ts` verifies private files, idempotence, conflict preservation and explicit disclosure.                                                                                                                                                                                                                          |
| AH-OPS-004   | Implemented | `src/cli/commands.test.ts` verifies isolation and duplicate ownership; compiled smoke queries live sessions.                                                                                                                                                                                                                            |
| AH-OPS-005   | Implemented | `src/cli/commands.test.ts` verifies launchd/systemd definitions and uninstall preservation; compiled smoke verifies signal teardown.                                                                                                                                                                                                    |
| AH-OPS-006   | Implemented | `src/cli/commands.ts` metadata-only diagnostics and bounded logs; compiled smoke checks logged credentials are absent.                                                                                                                                                                                                                  |
| AH-OPS-007   | Implemented | `scripts/build-cli.ts` and `.github/workflows/cli.yml` build four targets and run native acceptance; macOS arm64 isolated smoke passed locally, other native jobs require CI.                                                                                                                                                           |
| AH-OPS-008   | Implemented | `scripts/smoke-cli.ts` exercises the compiled runtime without Node or Deno on PATH.                                                                                                                                                                                                                                                     |
